**What this changes**

**Why**

**Checklist**
- [ ] `npm run lint`, `npm run typecheck`, `npm test` and `npm run build` pass
- [ ] Schema changes are a new migration in `pb_migrations/` with both `up` and `down`
- [ ] Tested against a fresh `pb_data` as well as an existing one
